# Local Protocol

Versioned protocol contracts shared by `graph-explorer-desktop` and `gx`.

## Versioning

- Current version: `v1`
- Compatibility policy:
  - additive fields are allowed within a major version
  - breaking changes require a new version directory

## Contents

- `v1/schema.json`: endpoint and event envelope schema (initial baseline)


Create two groups: on one group add all the nodes whose label ends 
with an even number and on the other group add the rest


create a new diagram with many nodes: one per shape style available with DOT


add a border to all nodes

Right now the internal phases go something like this:

1. `sourceText <-> versionedText`
2. `versionedText <-> versionedFullGraphV`
3. `versionedFullGraphV <-> fullGraphV`
4. `fullGraphV --> visibleGraph`
5. `visibleGraph -> visibleDOT`
6. `visibleDOT -> finalSVG`



